name: 'data_scrapers'
version: '1.0.0'

profile: 'data_scrapers'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  data_scrapers:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
